# eval/metrics

평가지표 정의와 계산 의미를 둔다. 구현 모듈이 지표를 자기 편의대로 재정의하지 않도록 eval Owner가 관리한다.

- `metric-definitions.md` — **확정된 지표의 계산 정의.** 분자·분모, 적중 판정, `null` 이 되는 조건, 버전 규칙. 지표 **목록**은 여기가 아니라 `architecture/module-architecture.md` §9-3 이 소유한다
