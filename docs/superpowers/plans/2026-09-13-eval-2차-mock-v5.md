# `eval` 2차 완료 (Mock Pack v5) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Mock Pack v5의 시나리오 7개를 `eval` 하니스가 끝까지 태우고, candidate·plate·cost 지표를 실제 수치로 내며, 결과 파일만으로 채점 조건을 재현할 수 있게 만든다.

**Architecture:** `impl → normalize → scorer → results`라는 기존 4단 구조를 그대로 쓴다. impl은 팀 공용 fixture(`data/mock/<module>/scenario_*.json`)를 **읽기만** 하고 다른 모듈 코드를 import하지 않는다. 정답지는 `data/mock/expected/`(eval 소유)에 `eval-expected/v2`로 두고, 채점에 쓰는 GT는 `eval/manifests/mock_pack/gt/`에 둔다. 이번에 stage가 `candidate`·`classification`에서 `plate`까지 셋으로 늘고, 결과 파일에 `cost` 블록이 새로 생긴다.

**Tech Stack:** Python 3.11 · pytest · 표준 라이브러리만(외부 의존 없음) · JSON fixture

**Spec:** `docs/superpowers/specs/2026-09-13-eval-2차-mock-v5-design.md`

## Global Constraints

이 절은 모든 Task의 요구사항에 암묵적으로 포함된다.

- **`data/mock/validate_mock_pack.py`를 수정하지 않는다.** 유소연 소유 공용 스크립트다. 새 정답지는 현행 §13을 **그대로 통과**해야 한다 — `provisional_non_contract_schema: true` + `metric_targets` 비어있지 않음 + 모든 `{kind, ref}`가 pack 안에서 해석될 것.
- **`data/mock/` 아래에서 `expected/`만 건드린다.** 다른 모듈의 fixture(`common/`·`recording/`·`search/`·`readout/`·`case/`·`evidence/`)는 읽기 전용이다.
- **값 복사 금지.** 정답지는 `ref`로 가리키고 비교 대상은 실행 시 역참조한다. `actual_value` 같은 사본 필드를 만들지 않는다.
- **검증할 GT가 없는 분기를 만들지 않는다** (harness-v1-design.md §9 F6). 참값이 없으면 `null` + 사유이지 `0`이 아니다.
- **mock tier 수치는 성능 근거가 아니다.** GT가 채점 대상과 같은 fixture에서 왔다. 순환성 경고를 GT `meta`에 반드시 남긴다.
- **통화가 섞이면 환산하지 말고 거부한다.** 환율은 `pricing_context`에 귀속되며 eval이 정할 값이 아니다.
- **커밋 메시지 끝에 반드시** `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`
- **Windows 한글 출력**: Git Bash에서 `PYTHONIOENCODING=utf-8 python ...`, PowerShell에서 `$env:PYTHONIOENCODING='utf-8'; python ...`
- **push하지 않는다.** 커밋만 한다.

## File Structure

**Create**

| 경로 | 책임 |
| --- | --- |
| `data/mock/expected/scenario_<7종>.expected.json` | eval 소유 정답 라벨. 값이 아니라 ref + 참값 |
| `eval/manifests/mock_pack/gt/gt_plate.json` | plate 채점용 GT(판독 단위) |
| `eval/scorers/cost.py` | 비용 집계. 파일을 읽지 않고 envelope의 `facts`만 본다 |
| `tests/eval/test_expected_labels.py` | 라벨이 스스로 앞뒤가 맞는지 + 파생 라벨이 pack과 갈라지지 않았는지 |
| `tests/eval/test_scorer_plate.py` | plate 교차표 |
| `tests/eval/test_scorer_cost.py` | 비용 집계와 통화 거부 |

**Modify**

| 경로 | 무엇을 |
| --- | --- |
| `eval/scorers/candidate.py` | 제외 어휘(Task 1) · 점 오차 매처와 `_iou` 삭제(Task 4) |
| `eval/runners/normalize.py` | `representative_sec`·`timeline_revision` 추가, `normalize_plate` 신설, 죽은 `from_mock_pack` 제거 |
| `eval/runners/impls/mock_pack.py` | 7개 시나리오 순회 · plate stage · `run_facts` 훅 |
| `eval/run.py` | `plate` stage · `contract_version`·`processed_duration_sec`·`facts` |
| `eval/score.py` | `scorer_version`·`prediction_ref` · 계약 버전 비교 거부 · `cost` 블록 |
| `eval/scorers/plate.py` | GT 분기 재구현 |
| `eval/manifests_io.py` | `sha256_file` 공개 |
| `eval/manifests/mock_pack/gt/gt_candidate.json` | 7개 항목 · onset 기준 재생성 |
| `tests/eval/test_mock_pack_contract.py` · `test_mock_pack_pipeline.py` · `test_normalize.py` · `test_scorer_candidate.py` | 개명과 새 동작 반영 |
| `docs/modules/eval/experiment-guide.md` · `harness-v1-design.md` | 문구 2곳 |

**Delete**: `data/mock/expected/eval_fixture_correct_001.json` · `eval_fixture_wrong_001.json`

---

## Task 1: 제외 어휘를 두 이름으로 가른다

**Files:**
- Modify: `eval/scorers/candidate.py:33-110`
- Test: `tests/eval/test_scorer_candidate.py`

**Interfaces:**
- Consumes: 없음(첫 Task)
- Produces: `candidate.SCORING_VALUES = ("INCLUDED", "EXCLUDED", "BOUNDARY_EXCLUDED")` · `candidate.score()` 결과에 새 키 `excluded_by_reason: dict[str, int] | None`

**왜:** 지금 `candidate.py:40`은 `BOUNDARY_EXCLUDED`만 제외한다. `unknown_abstain_partial_001`의 라벨은 참값 유형이 없어 `EXCLUDED`인데, 그대로 두면 채점 대상에 들어가 **영원히 맞출 수 없는 miss**가 되고 recall이 부당하게 낮아진다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`tests/eval/test_scorer_candidate.py` 끝에 추가:

```python
def test_two_exclusion_reasons_are_counted_separately():
    """제외 사유를 합산하지 않는다.

    BOUNDARY_EXCLUDED(클립 경계에 걸침)와 EXCLUDED(참값 유형 자체가 없음)는
    다른 사실이다. 결과 파일만 보고 n_events 가 GT 와 어긋난 이유를 읽을 수
    있어야 하므로 사유별로 남긴다.
    """
    gt = {"items": [{"clip_id": "c1", "targets": [
        {"violation_type": "SIGNAL", "t_start_sec": 10.0, "t_end_sec": 12.0,
         "t_onset_sec": 10.5, "scoring": "INCLUDED"},
        {"violation_type": None, "t_start_sec": 20.0, "t_end_sec": 22.0,
         "t_onset_sec": 20.5, "scoring": "EXCLUDED"},
        {"violation_type": "SIGNAL", "t_start_sec": 30.0, "t_end_sec": 32.0,
         "t_onset_sec": 30.5, "scoring": "BOUNDARY_EXCLUDED"},
    ]}]}
    normalized = [{"clip_id": "c1", "candidates": []}]

    out = candidate.score(normalized, gt)

    assert out["n_events"] == 1
    assert out["excluded_by_reason"] == {"EXCLUDED": 1, "BOUNDARY_EXCLUDED": 1}


def test_scoring_defaults_to_included_when_absent():
    """scoring 이 없는 target 은 채점 대상이다.

    기존 정답지에 scoring 이 없을 수 있고, 없다고 조용히 빼면 분모가 줄어
    recall 이 부풀려진다.
    """
    gt = {"items": [{"clip_id": "c1", "targets": [
        {"violation_type": "SIGNAL", "t_start_sec": 10.0, "t_end_sec": 12.0,
         "t_onset_sec": 10.5},
    ]}]}
    out = candidate.score([{"clip_id": "c1", "candidates": []}], gt)

    assert out["n_events"] == 1
    assert out["excluded_by_reason"] == {}


def test_unknown_scoring_value_raises_instead_of_silently_excluding():
    """오타가 조용한 제외로 둔갑하지 않게 한다."""
    gt = {"items": [{"clip_id": "c1", "targets": [
        {"violation_type": "SIGNAL", "t_start_sec": 10.0, "t_end_sec": 12.0,
         "t_onset_sec": 10.5, "scoring": "EXCLUDE"},
    ]}]}
    with pytest.raises(ValueError, match="EXCLUDE"):
        candidate.score([{"clip_id": "c1", "candidates": []}], gt)
```

파일 맨 위 import에 `pytest`가 없으면 추가한다:

```python
import pytest
```

- [ ] **Step 2: 실패를 확인한다**

Run: `python -m pytest tests/eval/test_scorer_candidate.py -q -k "exclusion or defaults_to_included or unknown_scoring"`
Expected: 3건 FAIL — `KeyError: 'excluded_by_reason'` 2건, `Failed: DID NOT RAISE` 1건

- [ ] **Step 3: 최소 구현**

`eval/scorers/candidate.py`의 `_iou` 함수 **아래**에 추가:

```python
SCORING_VALUES = ("INCLUDED", "EXCLUDED", "BOUNDARY_EXCLUDED")


def _partition(targets, where):
    """채점 대상과 제외 사유별 개수로 가른다.

    scoring 이 없으면 INCLUDED 로 본다 — 없다고 빼면 분모가 조용히 줄어
    recall 이 부풀려진다. 모르는 값은 채우지 않고 예외로 올린다.
    """
    included = []
    excluded = {}
    for t in targets:
        scoring = t.get("scoring", "INCLUDED")
        if scoring not in SCORING_VALUES:
            raise ValueError("%s: 알 수 없는 scoring %r" % (where, scoring))
        if scoring == "INCLUDED":
            included.append(t)
        else:
            excluded[scoring] = excluded.get(scoring, 0) + 1
    return included, excluded
```

`score()` 안에서 39~46줄의 루프를 교체한다:

```python
    events = []          # (clip_id, target)
    negative_clips = []
    excluded_by_reason = {}
    for item in gt["items"]:
        included, excluded = _partition(item["targets"], item["clip_id"])
        for reason, n in excluded.items():
            excluded_by_reason[reason] = excluded_by_reason.get(reason, 0) + n
        if item["targets"]:
            for t in included:
                events.append((item["clip_id"], t))
        else:
            negative_clips.append(item["clip_id"])
```

같은 함수의 `n_boundary_excluded` 사유 블록을 교체한다:

```python
    for reason, n in sorted(excluded_by_reason.items()):
        # 이걸 적지 않으면 결과의 n_events 와 GT 의 clips_with_events 가
        # 어긋난 이유를 결과 파일만 보고는 알 수 없다 (스펙 §5).
        reasons.append("%s — %d건을 채점에서 제외했다" % (reason, n))
```

반환 dict에 키를 더한다(`"n_negative_clips"` 바로 아래):

```python
        "excluded_by_reason": excluded_by_reason,
```

`not_run()` 반환 dict에도 더한다(`"n_negative_clips": None` 아래):

```python
        "excluded_by_reason": None,
```

`n_boundary_excluded` 변수 선언(38줄)은 이제 쓰이지 않으므로 **지운다.**

- [ ] **Step 4: 통과를 확인한다**

Run: `python -m pytest tests/eval/test_scorer_candidate.py -q`
Expected: PASS (기존 테스트 포함 전부)

- [ ] **Step 5: 커밋**

```bash
git add eval/scorers/candidate.py tests/eval/test_scorer_candidate.py
git commit -F - <<'EOF'
feat(eval): 제외 사유를 EXCLUDED / BOUNDARY_EXCLUDED 로 가른다

참값 유형이 없어 제외하는 것과 클립 경계에 걸려 제외하는 것은 다른
사실이다. 합산하면 결과 파일만 보고 n_events 가 GT 와 어긋난 이유를
알 수 없다. 모르는 scoring 값은 조용히 제외하지 않고 예외로 올린다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
```

---

## Task 2: `eval-expected/v2` 정답지 7개

**Files:**
- Create: `data/mock/expected/scenario_happy_001.expected.json` 외 6개
- Delete: `data/mock/expected/eval_fixture_correct_001.json` · `eval_fixture_wrong_001.json`
- Test: `tests/eval/test_expected_labels.py` (신규)

**Interfaces:**
- Consumes: 없음
- Produces: `data/mock/expected/<scenario_id>.expected.json` — `{schema, scenario_id, provisional_non_contract_schema, metric_targets[]}`. `metric_targets[].metric`은 `candidate_onset` · `plate_readout` · `occurred_at` · `negative_clip` · `not_scored` 중 하나. Task 3(GT 재생성)과 Task 5(plate GT)가 이 값을 옮겨 적는다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`tests/eval/test_expected_labels.py` 신규 작성:

```python
"""expected/ 라벨이 스스로 앞뒤가 맞고 pack 과 갈라지지 않았는지 본다.

공용 validator §13 은 metric_targets 가 비어 있는지와 ref 가 해석되는지만
본다. 라벨 '내용'을 검증하는 자리는 여기다 (스펙 §2-1, §12).

**등식 검사는 derived_from_pack=true 인 라벨에만 건다.** 무조건 단언하면
mock impl 이 항상 옳다고 못 박는 꼴이 되어, wrong_accept_rate 의 분자를
메울 「일부러 틀린」 fixture 가 들어오는 순간 이 테스트가 먼저 깨진다.
abstained 도 단언하지 않는다 — 그것은 채점 대상이지 전제가 아니다.
"""
import json
import os

import pytest

from eval import paths

MOCK = os.path.join(paths.REPO_ROOT, "data", "mock")

SCENARIOS = (
    "scenario_happy_001",
    "scenario_empty_001",
    "scenario_plate_reread_001",
    "scenario_correction_rerun_001",
    "scenario_unknown_abstain_partial_001",
    "scenario_infra_failure_001",
    "scenario_relative_rebase_001",
)

METRICS = ("candidate_onset", "plate_readout", "occurred_at",
           "negative_clip", "not_scored")
SCORING_VALUES = ("INCLUDED", "EXCLUDED", "BOUNDARY_EXCLUDED")


def _read(*parts):
    path = os.path.join(MOCK, *parts)
    if not os.path.exists(path):
        return None
    with open(path, encoding="utf-8") as f:
        return json.load(f)


def _expected(scenario):
    return _read("expected", scenario + ".expected.json")


def _targets(scenario, metric):
    doc = _expected(scenario)
    return [t for t in doc["metric_targets"] if t["metric"] == metric]


def _plate_readouts(scenario):
    doc = _read("readout", scenario + ".json")
    if doc is None:
        return {}
    return {p["readout_id"]: p for p in doc.get("plate_readouts", [])}


def _candidates(scenario):
    doc = _read("search", scenario + ".json")
    if doc is None:
        return {}
    out = {}
    for ev in doc.get("analysis_run_candidate_events", []):
        for c in ev.get("candidates", []):
            out[c["candidate_id"]] = c
    return out


@pytest.mark.parametrize("scenario", SCENARIOS)
def test_every_scenario_has_an_expected_file(scenario):
    doc = _expected(scenario)
    assert doc is not None, "%s 의 정답지가 없다" % scenario
    assert doc["schema"] == "eval-expected/v2"
    assert doc["provisional_non_contract_schema"] is True
    assert doc["metric_targets"], "metric_targets 가 비면 공용 validator §13 이 FAIL 한다"
    for t in doc["metric_targets"]:
        assert t["metric"] in METRICS, "알 수 없는 metric %r" % t["metric"]


@pytest.mark.parametrize("scenario", SCENARIOS)
def test_plate_label_is_internally_coherent(scenario):
    """READABLE 이면 참값이 있고 UNREADABLE 이면 없다.

    라벨 자신의 앞뒤만 본다. 판독 결과와 맞는지는 별개 테스트다.
    """
    for t in _targets(scenario, "plate_readout"):
        if t["legibility"] == "READABLE":
            assert t.get("true_text"), "%s: READABLE 인데 true_text 가 없다" % t["ref"]["ref"]
        elif t["legibility"] == "UNREADABLE":
            assert "true_text" not in t, \
                "%s: UNREADABLE 인데 true_text 가 있다" % t["ref"]["ref"]
        else:
            pytest.fail("알 수 없는 legibility %r" % t["legibility"])


@pytest.mark.parametrize("scenario", SCENARIOS)
def test_derived_plate_labels_match_the_pack(scenario):
    """pack 에서 유도한 라벨만 원본과 같은지 본다.

    derived_from_pack=false 인 라벨(독립 참값·일부러 틀린 fixture)은
    건너뛴다. 여기서 등식을 강제하면 채점할 오답 자체를 만들 수 없다.
    """
    readouts = _plate_readouts(scenario)
    for t in _targets(scenario, "plate_readout"):
        if not t.get("derived_from_pack"):
            continue
        rid = t["ref"]["ref"]
        assert rid in readouts, "%s: pack 에 그런 판독이 없다" % rid
        if t["legibility"] == "READABLE":
            assert t["true_text"] == readouts[rid]["observation"]["value"], \
                "%s: 파생 라벨이 원본과 갈라졌다" % rid


@pytest.mark.parametrize("scenario", SCENARIOS)
def test_candidate_onset_matches_representative_ms(scenario):
    """mock tier onset 은 representative_ms 에서 유도한 값이다.

    성능 검사가 아니라 GT 재생성 누락 검사다 (스펙 §6-2 순환성 경고 참조).
    """
    cands = _candidates(scenario)
    for t in _targets(scenario, "candidate_onset"):
        cid = t["ref"]["ref"]
        assert cid in cands, "%s: pack 에 그런 candidate 가 없다" % cid
        span = cands[cid]["span"]
        assert t["onset_ms"] == span["representative_ms"]
        assert t["timeline_revision"] == span["timeline_revision"]


@pytest.mark.parametrize("scenario", SCENARIOS)
def test_scoring_values_are_known(scenario):
    for t in _targets(scenario, "candidate_onset"):
        assert t["scoring"] in SCORING_VALUES, "알 수 없는 scoring %r" % t["scoring"]


def test_old_id_comparison_fixtures_are_gone():
    """ID 비교 채점 모델은 폐기했다.

    「채점기가 오류를 잡는가」는 fake_always_correct / fake_always_wrong 두
    impl 이 실제 파이프라인을 통과시켜 이미 증명한다.
    """
    assert _read("expected", "eval_fixture_correct_001.json") is None
    assert _read("expected", "eval_fixture_wrong_001.json") is None
```

- [ ] **Step 2: 실패를 확인한다**

Run: `python -m pytest tests/eval/test_expected_labels.py -q`
Expected: FAIL — `%s 의 정답지가 없다` 7건 + `test_old_id_comparison_fixtures_are_gone` 1건

- [ ] **Step 3: 정답지 7개를 만들고 구 fixture 2개를 지운다**

`data/mock/expected/scenario_happy_001.expected.json`:

```json
{
  "schema": "eval-expected/v2",
  "scenario_id": "scenario_happy_001",
  "provisional_non_contract_schema": true,
  "note": "eval 소유 정답지. metric_targets 라는 이름은 공용 validator §13 과의 호환을 위해 유지한다. 값 사본을 싣지 않고 ref 로 가리킨다.",
  "metric_targets": [
    {
      "metric": "candidate_onset",
      "ref": { "kind": "candidate", "ref": "candidate_h001" },
      "violation_type": "SOLID_LINE_LANE_CHANGE",
      "onset_ms": 312480,
      "timeline_revision": 1,
      "scoring": "INCLUDED"
    },
    {
      "metric": "plate_readout",
      "ref": { "kind": "plate_readout", "ref": "readout_h001_plate" },
      "legibility": "READABLE",
      "true_text": "12가3456",
      "derived_from_pack": true
    },
    {
      "metric": "occurred_at",
      "ref": { "kind": "time_resolution", "ref": "tres_h001" },
      "expected": "2026-08-24T18:05:12+09:00"
    }
  ]
}
```

`data/mock/expected/scenario_empty_001.expected.json`:

```json
{
  "schema": "eval-expected/v2",
  "scenario_id": "scenario_empty_001",
  "provisional_non_contract_schema": true,
  "note": "음성 클립이다. 빈 배열로 두면 공용 validator §13 이 metric_targets is empty 로 FAIL 하고, 「라벨을 아직 안 만들었다」와도 구분되지 않는다. 음성을 라벨로 적는다.",
  "metric_targets": [
    {
      "metric": "negative_clip",
      "ref": { "kind": "recording_timeline", "ref": "tl_e001" },
      "expected_event_count": 0
    }
  ]
}
```

`data/mock/expected/scenario_plate_reread_001.expected.json`:

```json
{
  "schema": "eval-expected/v2",
  "scenario_id": "scenario_plate_reread_001",
  "provisional_non_contract_schema": true,
  "note": "plate 라벨은 판독 단위다. legibility 는 (incident_clip, source_profile) 속성이라 초판독(readout-native)과 재판독(readout-native-hires)의 정답이 달라도 모순이 아니다. 초판독의 abstain 은 실패가 아니라 정답이다.",
  "metric_targets": [
    {
      "metric": "candidate_onset",
      "ref": { "kind": "candidate", "ref": "candidate_p001" },
      "violation_type": "SIGNAL",
      "onset_ms": 612000,
      "timeline_revision": 1,
      "scoring": "INCLUDED"
    },
    {
      "metric": "plate_readout",
      "ref": { "kind": "plate_readout", "ref": "readout_p001_plate" },
      "legibility": "UNREADABLE",
      "derived_from_pack": true
    },
    {
      "metric": "plate_readout",
      "ref": { "kind": "plate_readout", "ref": "readout_p001_plate_reread" },
      "legibility": "READABLE",
      "true_text": "17나2867",
      "derived_from_pack": true
    },
    {
      "metric": "occurred_at",
      "ref": { "kind": "time_resolution", "ref": "tres_p001" },
      "expected": "2026-08-29T20:10:12+09:00"
    }
  ]
}
```

`data/mock/expected/scenario_correction_rerun_001.expected.json`:

```json
{
  "schema": "eval-expected/v2",
  "scenario_id": "scenario_correction_rerun_001",
  "provisional_non_contract_schema": true,
  "note": "시각 해석이 2개다. tres_r001_v1(NEEDS_REVIEW, 13:15:30)을 tres_r001_v2(OK, 13:13:00, user_corrected=true)가 대체했다. 정답은 v2 다. anchor 정정이므로 offset 930s 는 그대로다.",
  "metric_targets": [
    {
      "metric": "candidate_onset",
      "ref": { "kind": "candidate", "ref": "candidate_r001" },
      "violation_type": "MOTORCYCLE_HELMET_NON_USE",
      "onset_ms": 930000,
      "timeline_revision": 1,
      "scoring": "INCLUDED"
    },
    {
      "metric": "plate_readout",
      "ref": { "kind": "plate_readout", "ref": "readout_r001_plate" },
      "legibility": "READABLE",
      "true_text": "34나7890",
      "derived_from_pack": true
    },
    {
      "metric": "occurred_at",
      "ref": { "kind": "time_resolution", "ref": "tres_r001_v2" },
      "expected": "2026-08-27T13:13:00+09:00"
    }
  ]
}
```

`data/mock/expected/scenario_unknown_abstain_partial_001.expected.json`:

```json
{
  "schema": "eval-expected/v2",
  "scenario_id": "scenario_unknown_abstain_partial_001",
  "provisional_non_contract_schema": true,
  "note": "후보에 참값 유형이 없어 EXCLUDED 다 — 채점에 넣으면 영원히 맞출 수 없는 miss 가 된다. 시각은 「값 없음」이 아니라 「확정하면 오답」이다. fixture tres_u001 은 NEEDS_REVIEW 상태로 값을 싣고 있고, 충돌이 보존된 상태가 정답이다.",
  "metric_targets": [
    {
      "metric": "candidate_onset",
      "ref": { "kind": "candidate", "ref": "candidate_u001" },
      "violation_type": null,
      "onset_ms": 615000,
      "timeline_revision": 1,
      "scoring": "EXCLUDED"
    },
    {
      "metric": "plate_readout",
      "ref": { "kind": "plate_readout", "ref": "readout_u001_plate" },
      "legibility": "READABLE",
      "true_text": "88부1234",
      "derived_from_pack": true
    },
    {
      "metric": "occurred_at",
      "ref": { "kind": "time_resolution", "ref": "tres_u001" },
      "expected": null,
      "expected_status": "NEEDS_REVIEW"
    }
  ]
}
```

`data/mock/expected/scenario_infra_failure_001.expected.json`:

```json
{
  "schema": "eval-expected/v2",
  "scenario_id": "scenario_infra_failure_001",
  "provisional_non_contract_schema": true,
  "note": "search fixture 자체가 없다 — 「후보 없음」이 아니라 「대상 아님」이다. overlay 판독 2건은 v5 에서 추가됐으나 둘 다 UNKNOWN·value null 이라 시각 정답이 서지 않는다.",
  "metric_targets": [
    {
      "metric": "negative_clip",
      "ref": { "kind": "recording_timeline", "ref": "tl_x001" },
      "expected_event_count": 0
    },
    {
      "metric": "not_scored",
      "scope": "plate",
      "reason": "READOUT_INFRA_FAILURE"
    },
    {
      "metric": "not_scored",
      "scope": "occurred_at",
      "reason": "NO_TIME_RESOLUTION"
    }
  ]
}
```

`data/mock/expected/scenario_relative_rebase_001.expected.json`:

```json
{
  "schema": "eval-expected/v2",
  "scenario_id": "scenario_relative_rebase_001",
  "provisional_non_contract_schema": true,
  "note": "timeline 이 revision 2개다. candidate 는 rev 1 에 달려 있다. 절대 시각이 서지 않아(USABLE_RELATIVE_ONLY) 시각은 채점하지 않는다.",
  "metric_targets": [
    {
      "metric": "candidate_onset",
      "ref": { "kind": "candidate", "ref": "candidate_rb001" },
      "violation_type": "CENTER_LINE_CROSSING",
      "onset_ms": 512000,
      "timeline_revision": 1,
      "scoring": "INCLUDED"
    },
    {
      "metric": "not_scored",
      "scope": "plate",
      "reason": "NO_PLATE_READOUT"
    },
    {
      "metric": "not_scored",
      "scope": "occurred_at",
      "reason": "USABLE_RELATIVE_ONLY"
    }
  ]
}
```

구 fixture 2개를 지운다:

```bash
git rm data/mock/expected/eval_fixture_correct_001.json data/mock/expected/eval_fixture_wrong_001.json
```

- [ ] **Step 4: 테스트와 공용 validator를 둘 다 통과시킨다**

Run: `python -m pytest tests/eval/test_expected_labels.py -q`
Expected: PASS

Run: `python data/mock/validate_mock_pack.py`
Expected: `PASSED (exit 0)` — 파일 수가 46에서 51로 는다(구 2개 삭제, 신규 7개). **`[EVAL] ...: metric_targets is empty`가 하나도 없어야 한다.** 나오면 해당 시나리오의 `metric_targets`가 비어 있다는 뜻이므로 Step 3으로 돌아간다.

- [ ] **Step 5: 커밋**

```bash
git add data/mock/expected tests/eval/test_expected_labels.py
git commit -F - <<'EOF'
feat(eval): eval-expected/v2 정답지 7개 + 라벨 검증 테스트

값 사본을 싣지 않고 ref 로 가리킨다. 라벨 배열 이름을 metric_targets 로
유지해 공용 validator §13 을 수정 없이 통과한다.

음성 2건(empty·infra_failure)은 빈 배열이 아니라 negative_clip 라벨로
적는다 — 빈 배열은 §13 을 FAIL 시키고 「라벨을 아직 안 만들었다」와도
구분되지 않는다.

등식 검사는 derived_from_pack=true 라벨에만 걸고 abstained 는 단언하지
않는다. 무조건 단언하면 채점할 오답 자체를 만들 수 없게 된다.

ID 비교 채점 모델인 구 fixture 2개는 폐기한다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
```

---

## Task 3: impl을 7개 시나리오로 넓히고 GT를 재생성한다

**Files:**
- Modify: `eval/runners/impls/mock_pack.py` (전면 교체)
- Modify: `eval/runners/normalize.py:76-131`
- Modify: `eval/manifests/mock_pack/gt/gt_candidate.json` (전면 교체)
- Modify: `tests/eval/test_mock_pack_contract.py` · `tests/eval/test_mock_pack_pipeline.py` · `tests/eval/test_normalize.py`

**Interfaces:**
- Consumes: Task 2의 정답지 값
- Produces:
  - `mock_pack.SCENARIOS: tuple[str, ...]` — 7개
  - `mock_pack.run(scope)` — `stage="candidate"`면 `[{"clip_id": str, "candidates": [{"rank", "t_start_sec", "t_end_sec", "representative_sec", "timeline_revision", "event_type", "score"}]}]`
  - `normalize.from_candidate_events(raw)` 출력에 `representative_sec: float` · `timeline_revision: int` 추가
  - GT target 모양: `{"event_id", "scoring", "violation_type", "t_onset_sec"}` — **외연(`t_start_sec`/`t_end_sec`) 없음**

**왜:** 1개로 재배선한 뒤 곧바로 7개로 넓히는 것은 같은 코드를 두 번 고치는 일이다. 그리고 **`from_candidate_events`가 `representative_ms`를 아예 싣지 않아** Task 4의 점 오차 매처가 쓸 값이 파이프라인에 없다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`tests/eval/test_mock_pack_contract.py`의 26줄 `SCENARIOS`를 교체하고, 죽은 경로를 읽는 헬퍼 호출(110·136·156줄의 `_load("search", "candidate_events...")`)을 새 경로로 바꾼다. 파일 상단을 이렇게 만든다:

```python
SCENARIOS = (
    "scenario_happy_001",
    "scenario_empty_001",
    "scenario_plate_reread_001",
    "scenario_correction_rerun_001",
    "scenario_unknown_abstain_partial_001",
    "scenario_relative_rebase_001",
)
```

`scenario_infra_failure_001`은 search fixture가 없으므로 **candidate 계열 테스트의 대상이 아니다.**

같은 파일 165줄 docstring의 `scenario_partial_001`도 `scenario_unknown_abstain_partial_001`로 바꾼다. 개명이 끝났는지는 이것으로 확인한다:

```bash
grep -rn --exclude-dir=__pycache__ "scenario_partial_001\|cand_h001" eval/ tests/
```
출력이 없어야 한다.

`tests/eval/test_mock_pack_pipeline.py`에 추가:

```python
def test_impl_covers_every_scenario_that_has_a_search_fixture():
    """search fixture 가 없는 시나리오는 「후보 없음」이 아니라 「대상 아님」이다.

    둘을 뭉개면 infra_failure_001 이 음성 클립으로 분모에 들어가
    fp_per_clip 이 조용히 희석된다.
    """
    impl = registry.get("mock_pack:contracts")
    raw = impl({"manifest": MANIFEST, "stage": "candidate"})

    clip_ids = {item["clip_id"] for item in raw}
    assert "scenario_infra_failure_001" not in clip_ids
    assert len(raw) == 6
    # empty_001 은 fixture 가 있고 후보가 0건이다 — 이쪽은 진짜 음성이다
    empty = next(i for i in raw if i["clip_id"] == "scenario_empty_001")
    assert empty["candidates"] == []


def test_impl_carries_representative_ms_for_point_error_matching():
    """점 오차 매처가 쓸 값이 예측에 실려 오는지 본다.

    계약의 span 은 coarse 후보 창이고 사건 외연이 아니다
    (contract-analysis-run-candidate-event v1.1 §4-1). 매칭은
    representative_ms 로 한다.
    """
    impl = registry.get("mock_pack:contracts")
    raw = impl({"manifest": MANIFEST, "stage": "candidate"})

    happy = next(i for i in raw if i["clip_id"] == "scenario_happy_001")
    c = happy["candidates"][0]
    assert c["representative_sec"] == 312.48
    assert c["timeline_revision"] == 1
    assert c["event_type"] == "SOLID_LINE_LANE_CHANGE"


def test_gt_has_one_item_per_scenario_and_keeps_the_circularity_warning():
    gt = manifests_io.load_gt(MANIFEST, "candidate")
    cov = gt["meta"]["coverage"]

    assert len(gt["items"]) == 7
    assert cov["clips_total"] == 7
    assert cov["clips_with_events"] == 5
    assert cov["derived_from_mock_pack"] is True
    assert cov["independent_ground_truth"] is False
    assert "순환" in cov["warning"]
    # 죽은 경로를 가리키지 않는다
    for p in cov["derived_from"]:
        assert os.path.exists(os.path.join(paths.REPO_ROOT, p.split(" ")[0])), p


def test_empty_scenario_makes_fp_per_clip_a_real_number(tmp_path, monkeypatch):
    """empty_001 이 pack 최초 음성 케이스다.

    fp_per_clip 이 null(NO_NEGATIVE_CLIPS)에서 처음으로 실제 값이 된다.
    """
    monkeypatch.setattr(paths, "predictions_dir", lambda: str(tmp_path / "predictions"))
    monkeypatch.setattr(paths, "results_dir", lambda: str(tmp_path / "results"))

    assert run.main(["--impl", "mock_pack:contracts", "--manifest", MANIFEST,
                     "--stage", "candidate", "--run-id", "t_seven"]) == 0
    assert score.main(["--prediction", "t_seven"]) == 0

    gt = manifests_io.load_gt(MANIFEST, "candidate")
    out = tmp_path / "results" / ("t_seven.%s.json" % gt["meta"]["gt_version"])
    cand = json.loads(out.read_text(encoding="utf-8"))["candidate"]

    assert cand["fp_per_clip"] == 0.0
    assert cand["n_negative_clips"] == 1
    assert cand["excluded_by_reason"] == {"EXCLUDED": 1}
```

`test_mock_pack_pipeline.py` 상단 import에 `os`를 추가한다.

기존 `test_impl_reads_team_fixture_and_keeps_the_contract_span`(35~50줄)은 690.0/708.0을 단언한다. 이 값은 coarse 창이지 사건 외연이 아니므로 **테스트 이름과 본문을 교체한다**:

```python
def test_impl_reads_team_fixture_and_keeps_the_coarse_window():
    """impl 이 서어진의 계약 산출물을 읽고 coarse 창을 살려 오는지 확인한다.

    span 은 「이 근처를 보라」는 창이지 사건의 외연이 아니다. 창은 보조
    신호(containment)로만 쓰고 매칭은 representative_ms 로 한다.
    """
    impl = registry.get("mock_pack:contracts")
    raw = impl({"manifest": MANIFEST, "stage": "candidate"})

    happy = next(i for i in raw if i["clip_id"] == SCENARIO)
    c = happy["candidates"][0]
    assert c["t_start_sec"] == 300.0
    assert c["t_end_sec"] == 420.0
    assert c["t_start_sec"] <= c["representative_sec"] <= c["t_end_sec"]
```

`tests/eval/test_normalize.py`에서 `cand_h001`을 `candidate_h001`로 바꾸고, `from_candidate_events` 출력 검사에 두 필드를 더한다:

```python
    assert out[0]["representative_sec"] == 312.48
    assert out[0]["timeline_revision"] == 1
```

- [ ] **Step 2: 실패를 확인한다**

Run: `python -m pytest tests/eval -q`
Expected: FAIL — `FileNotFoundError: ...candidate_events.happy_001.json` 및 `KeyError: 'representative_sec'`

- [ ] **Step 3: 구현**

`eval/runners/normalize.py`의 `from_candidate_events`에서 반환 dict에 두 필드를 더한다:

```python
            "representative_sec": _require_field(
                span, "representative_ms", "%s.span" % where) / 1000.0,
            "timeline_revision": span.get("timeline_revision"),
```

docstring 첫 문단 아래에 한 줄 더한다:

```python
    span.start_ms/end_ms 는 coarse 후보 창이고 사건 외연이 아니다. 매칭에
    쓰는 값은 representative_ms 이며(계약 v1.1 §4-1), 창은 containment
    보조 신호로만 쓴다.
```

같은 파일의 `from_mock_pack`(108~131줄)을 **통째로 지운다** — 읽던 `data/mock/eval/prediction_*.json`이 소멸했다.

`eval/runners/normalize.py`의 `normalize_candidate`가 두 필드를 떨어뜨리지 않게 46~56줄의 dict comprehension을 고친다:

```python
            "candidates": [
                {"rank": k + 1,
                 "t_start_sec": float(c["t_start_sec"]),
                 "t_end_sec": float(c["t_end_sec"]),
                 "representative_sec": float(c["representative_sec"]),
                 "timeline_revision": c.get("timeline_revision"),
                 "event_type": c["event_type"],
                 "score": float(c["score"])}
                for k, c in enumerate(cands)
            ],
```

40~44줄의 필수 필드 검사에 한 줄 더한다:

```python
            _require_field(c, "representative_sec", cwhere)
```

`eval/runners/impls/mock_pack.py`를 전면 교체한다:

```python
"""팀 공용 Mock Pack 산출물을 읽는 impl.

`fake:` 접두어를 쓰지 않는다 — 정답지를 몰래 읽는 치트가 아니라 다른 Owner 가
만든 계약 산출물을 읽는다. 실제 구현이 붙기 전까지 그 자리를 대신한다.

`eval` 은 `case`·`search`·`readout` 의 코드를 import 하지 않는다. 파일만 읽는다.
"""
import json
import os

from eval import paths
from eval.runners import normalize

IMPL_VERSION = "v2"

SCENARIOS = (
    "scenario_happy_001",
    "scenario_empty_001",
    "scenario_plate_reread_001",
    "scenario_correction_rerun_001",
    "scenario_unknown_abstain_partial_001",
    "scenario_infra_failure_001",
    "scenario_relative_rebase_001",
)

_MOCK = os.path.join(paths.REPO_ROOT, "data", "mock")


def _read(*parts):
    """없으면 None. 「파일이 없다」와 「내용이 비었다」는 다른 사실이다."""
    path = os.path.join(_MOCK, *parts)
    if not os.path.exists(path):
        return None
    with open(path, encoding="utf-8") as f:
        return json.load(f)


def _candidate_events(scenario):
    """search fixture 의 candidates 를 계약 모양 그대로 모은다."""
    doc = _read("search", scenario + ".json")
    if doc is None:
        return None
    out = []
    for ev in doc.get("analysis_run_candidate_events", []):
        out.extend(ev.get("candidates", []))
    return out


def run(scope):
    """계약 산출물을 stage 별 raw 로 옮긴다.

    scope 는 {"manifest", "stage"} 만 담는다 — 정답지는 넘어오지 않고,
    이 impl 도 정답지를 열지 않는다.

    clip_id 자리에 scenario_id 를 쓴다. 계약은 timeline_id + 밀리초 offset
    으로 위치를 말하고 정답지는 clip_id 로 말하는데, mock tier 는 시나리오당
    timeline 이 하나라 1:1 이 성립한다. 이 규약은 정답지 meta 에 적혀 있다.
    """
    if scope["stage"] == "candidate":
        return _run_candidate()
    raise ValueError(
        "mock_pack:contracts 는 stage=candidate 만 지원한다 (받은 값: %r)."
        % scope["stage"]
    )


def _run_candidate():
    out = []
    for scenario in SCENARIOS:
        events = _candidate_events(scenario)
        if events is None:
            # search fixture 가 없다 = 「대상 아님」. 「후보 없음」과 구분한다.
            # 뭉개면 음성 클립 수가 늘어 fp_per_clip 이 조용히 희석된다.
            continue
        cands = normalize.from_candidate_events(events)
        out.append({
            "clip_id": scenario,
            "candidates": [{
                "rank": e["rank"],
                "t_start_sec": e["t_start_sec"],
                "t_end_sec": e["t_end_sec"],
                "representative_sec": e["representative_sec"],
                "timeline_revision": e["timeline_revision"],
                "event_type": e["event_type"],
                "score": e["score"],
            } for e in cands],
        })
    return out
```

`eval/manifests/mock_pack/gt/gt_candidate.json`을 전면 교체한다:

```json
{
  "meta": {
    "gt_version": "mp1",
    "tier": "MOCK",
    "stage": "candidate",
    "clip_id_convention": "mock tier 는 scenario_id 를 clip_id 로 쓴다. 시나리오당 recording_timeline 이 하나라 1:1 이 성립한다. B tier 는 clip_id 가 실재하므로 같은 scorer 를 타더라도 의미가 다르다.",
    "coverage": {
      "clips_total": 7,
      "clips_with_events": 5,
      "negative_clips": 1,
      "not_applicable_clips": 1,
      "derived_from_mock_pack": true,
      "independent_ground_truth": false,
      "purpose": "팀 목데이터가 eval 파이프라인(run -> normalize -> score)을 끝까지 통과하는지 확인한다. 예측이 맞는지를 재는 정답지가 아니다.",
      "derived_from": [
        "data/mock/expected/ (eval-expected/v2 라벨)",
        "data/mock/search/ (candidate span.representative_ms)"
      ],
      "warning": "onset 은 채점 대상인 예측과 같은 fixture(span.representative_ms)에서 왔다. 따라서 onset_error 와 recall 은 순환적이며 성능 근거가 될 수 없다. 지표가 맞다는 증거는 b_youtube(55클립)·a_aihub(120시퀀스)에서 가짜 구현 2종의 점수 대비로 나온다.",
      "reviewed_by": "daewon",
      "reviewed_at": "2026-09-13"
    }
  },
  "items": [
    {
      "clip_id": "scenario_happy_001",
      "source_video_id": "tl_h001",
      "targets": [
        { "event_id": "EV_MP_H001", "scoring": "INCLUDED",
          "violation_type": "SOLID_LINE_LANE_CHANGE", "t_onset_sec": 312.48 }
      ]
    },
    {
      "clip_id": "scenario_empty_001",
      "source_video_id": "tl_e001",
      "targets": []
    },
    {
      "clip_id": "scenario_plate_reread_001",
      "source_video_id": "tl_p001",
      "targets": [
        { "event_id": "EV_MP_P001", "scoring": "INCLUDED",
          "violation_type": "SIGNAL", "t_onset_sec": 612.0 }
      ]
    },
    {
      "clip_id": "scenario_correction_rerun_001",
      "source_video_id": "tl_r001",
      "targets": [
        { "event_id": "EV_MP_R001", "scoring": "INCLUDED",
          "violation_type": "MOTORCYCLE_HELMET_NON_USE", "t_onset_sec": 930.0 }
      ]
    },
    {
      "clip_id": "scenario_unknown_abstain_partial_001",
      "source_video_id": "tl_u001",
      "targets": [
        { "event_id": "EV_MP_U001", "scoring": "EXCLUDED",
          "violation_type": null, "t_onset_sec": 615.0 }
      ]
    },
    {
      "clip_id": "scenario_infra_failure_001",
      "source_video_id": "tl_x001",
      "targets": [],
      "not_applicable": "search fixture 가 없다 — 후보 없음이 아니라 대상 아님이다"
    },
    {
      "clip_id": "scenario_relative_rebase_001",
      "source_video_id": "tl_rb001",
      "targets": [
        { "event_id": "EV_MP_RB001", "scoring": "INCLUDED",
          "violation_type": "CENTER_LINE_CROSSING", "t_onset_sec": 512.0 }
      ]
    }
  ]
}
```

**주의:** `infra_failure_001`은 GT에 항목으로 남되 impl이 예측을 내지 않는다. `candidate.score`는 `targets: []`인 항목을 음성 클립으로 세므로, **이대로 두면 음성 클립이 2개가 되어 위 테스트(`n_negative_clips == 1`)가 깨진다.** `candidate.score`에서 `not_applicable`이 있는 항목을 음성에서 빼야 한다. `score()`의 루프에 분기를 더한다:

```python
    for item in gt["items"]:
        if item.get("not_applicable"):
            # 예측을 만들 입력 자체가 없었다. 음성(=후보를 냈어야 하는데
            # 안 냈다)과 다르므로 fp_per_clip 의 분모에 넣지 않는다.
            continue
        included, excluded = _partition(item["targets"], item["clip_id"])
        ...
```

- [ ] **Step 4: 통과를 확인한다**

Run: `python -m pytest tests/eval -q`
Expected: **0 failed** — 이것이 Task 3의 완료 판정이다.

Run: `python data/mock/validate_mock_pack.py`
Expected: PASSED (exit 0)

- [ ] **Step 5: 커밋**

```bash
git add eval/runners/impls/mock_pack.py eval/runners/normalize.py eval/scorers/candidate.py \
        eval/manifests/mock_pack/gt/gt_candidate.json tests/eval/
git commit -F - <<'EOF'
feat(eval): mock_pack impl 을 7개 시나리오로 넓히고 GT 를 onset 기준으로 재생성

죽은 경로(search/candidate_events.*.json)를 계약 산출물 경로로 바꾸고
시나리오 상수 1개를 7개로 넓혔다. pytest 0 failed 를 회복한다.

search fixture 가 없는 infra_failure_001 은 「후보 없음」이 아니라
「대상 아님」으로 구분해 fp_per_clip 의 분모에서 뺀다. 뭉개면 분모가
조용히 희석된다.

from_candidate_events 가 representative_ms 를 싣지 않아 점 오차 매처가
쓸 값이 파이프라인에 없었다. representative_sec 과 timeline_revision 을
normalize 출력과 예측 항목에 추가한다.

empty_001 이 pack 최초 음성 케이스다 — fp_per_clip 이 null 에서 처음으로
실제 값이 된다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
```

---

## Task 4: 매칭을 IoU에서 onset point error로 바꾼다

**Files:**
- Modify: `eval/scorers/candidate.py`
- Test: `tests/eval/test_scorer_candidate.py`

**Interfaces:**
- Consumes: Task 3의 `representative_sec` · GT의 `t_onset_sec`
- Produces: `candidate.SCORER_VERSION = "s2"` · `candidate.DEFAULT_TOLERANCE_SEC = 2.0` · 결과 키 `onset_error_sec`(구 `span_error_sec`) · 새 키 `containment_rate` · `_iou` **삭제**

**왜:** `span`은 coarse 후보 창이지 사건 외연이 아니다(계약 v1.1 §4-1, 2026-09-10). GT target에서 외연이 사라지고 `t_onset_sec`만 남았으므로 **IoU는 계산 자체가 불가능**하다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`tests/eval/test_scorer_candidate.py`에 추가:

```python
def _gt(onset, violation_type="SIGNAL"):
    return {"items": [{"clip_id": "c1", "targets": [
        {"event_id": "E1", "scoring": "INCLUDED",
         "violation_type": violation_type, "t_onset_sec": onset},
    ]}]}


def _pred(rep, start, end, event_type="SIGNAL", score=0.9):
    return [{"clip_id": "c1", "candidates": [
        {"rank": 1, "t_start_sec": start, "t_end_sec": end,
         "representative_sec": rep, "timeline_revision": 1,
         "event_type": event_type, "score": score},
    ]}]


def test_match_is_decided_by_onset_point_error_not_overlap():
    """창이 넓어도 대표 시점이 멀면 적중이 아니다.

    구 IoU 매처는 창이 넓을수록 유리해 「아무 데나 넓게 잡기」를 보상했다.
    """
    out = candidate.score(_pred(rep=500.0, start=0.0, end=1000.0), _gt(100.0))
    assert out["recall_at"]["1"] == 0.0


def test_match_succeeds_within_tolerance_even_if_window_is_narrow():
    out = candidate.score(_pred(rep=101.5, start=101.0, end=102.0), _gt(100.0))
    assert out["recall_at"]["1"] == 1.0
    assert out["onset_error_sec"]["mean"] == pytest.approx(1.5)


def test_containment_is_reported_separately_from_the_match():
    """점 오차는 맞는데 창이 onset 을 안 품는 경우를 따로 본다.

    coarse 창 생성이 의심스러운 신호이므로 매칭 성패와 섞지 않는다.
    """
    out = candidate.score(_pred(rep=100.5, start=100.4, end=100.6), _gt(100.0))
    assert out["recall_at"]["1"] == 1.0
    assert out["containment_rate"] == 0.0


def test_span_error_sec_key_is_gone():
    """이름이 구간 오차를 뜻하는 채로 점 오차를 담으면 반드시 오해된다."""
    out = candidate.score(_pred(rep=100.0, start=90.0, end=110.0), _gt(100.0))
    assert "span_error_sec" not in out
    assert "onset_error_sec" in out
```

- [ ] **Step 2: 실패를 확인한다**

Run: `python -m pytest tests/eval/test_scorer_candidate.py -q`
Expected: FAIL — `KeyError: 'onset_error_sec'` 등

- [ ] **Step 3: 구현**

`eval/scorers/candidate.py`에서 `_iou` 함수(15~30줄)를 **삭제**하고 아래를 넣는다:

```python
SCORER_VERSION = "s2"   # 2026-09-13 IoU -> onset point error (계약 v1.1 §4-1)
DEFAULT_TOLERANCE_SEC = 2.0


def _contains(c, onset_sec):
    """coarse 창이 정답 시점을 품는가. 매칭 조건이 아니라 보조 신호다."""
    return c["t_start_sec"] <= onset_sec <= c["t_end_sec"]
```

모듈 docstring의 `span_error_sec` 문단을 교체한다:

```python
단, onset_error_sec 는 두 문서 모두 이름만 있고 정의가 없어 여기서 정한다:
recall_at[max(ks)] 계산에서 실제로 적중(matched)한 예측만을 대상으로,
예측의 대표 시점(representative_sec)과 GT onset 의 절대 오차를 잰다.
표본 수는 예측 개수가 아니라 「적중한 사건 수」다 — 매칭되지 않은 예측의
오차는 무엇과 비교해야 할지 정의되지 않으므로 넣지 않는다.

2026-09-10 계약 v1.1 §4-1 이 span 을 coarse 후보 창으로 확정하면서 IoU
매칭을 폐기했다. 창은 containment_rate 로만 남는다.
```

`score()` 시그니처를 바꾼다:

```python
def score(normalized, gt, ks=(1, 3, 10), tolerance_sec=DEFAULT_TOLERANCE_SEC):
```

매칭 루프를 교체한다:

```python
    for clip_id, t in events:
        vt = t["violation_type"]
        onset = t["t_onset_sec"]
        slot = by_type.setdefault(vt, {"hits": {k: 0 for k in ks}, "n": 0})
        slot["n"] += 1
        cands = by_clip.get(clip_id, [])
        matched_at_loosest = None
        for k in ks:
            topk = [c for c in cands if c["rank"] <= k]
            matched = next(
                (c for c in topk
                 if c["event_type"] == vt
                 and abs(c["representative_sec"] - onset) <= tolerance_sec),
                None,
            )
            if matched is not None:
                hits[k] += 1
                slot["hits"][k] += 1
                if k == loosest_k:
                    matched_at_loosest = matched
        if matched_at_loosest is not None:
            onset_errors.append(abs(matched_at_loosest["representative_sec"] - onset))
            contained.append(_contains(matched_at_loosest, onset))
```

`span_errors = []`를 `onset_errors = []`와 `contained = []`로 바꾸고, 사유 블록과 반환 dict를 고친다:

```python
    if n_events > 0 and not onset_errors:
        reasons.append("NO_MATCHED_EVENTS — onset_error_sec 를 낼 수 없다")
```

```python
        "onset_error_sec": {
            "mean": statistics.fmean(onset_errors) if onset_errors else None,
            "median": statistics.median(onset_errors) if onset_errors else None,
            "tolerance_sec": tolerance_sec,
        },
        "containment_rate": (sum(contained) / len(contained)) if contained else None,
```

`not_run()`도 같은 키로 맞춘다:

```python
        "onset_error_sec": {"mean": None, "median": None, "tolerance_sec": None},
        "containment_rate": None,
```

**기존 호출부를 먼저 찾는다** — `score()`의 키워드 인자가 `iou_threshold`에서 `tolerance_sec`로 바뀌므로, 넘기는 쪽이 있으면 `TypeError`가 난다.

```bash
grep -rn --exclude-dir=__pycache__ "iou_threshold\|span_error_sec" eval/ tests/
```

나온 곳을 전부 고친다 — `iou_threshold=...`를 넘기던 테스트는 `tolerance_sec=...`로 바꾸고(임계의 의미가 겹침 비율에서 **초 단위 허용 오차**로 달라졌으므로 값도 다시 정한다), `span_error_sec`를 읽던 단언은 `onset_error_sec`로 바꾼다.

- [ ] **Step 4: 통과를 확인한다**

Run: `python -m pytest tests/eval -q`
Expected: 0 failed

Run: `grep -rn "_iou\|span_error_sec" eval/ tests/`
Expected: 출력 없음 (고아가 남지 않았다)

- [ ] **Step 5: 커밋**

```bash
git add eval/scorers/candidate.py tests/eval/
git commit -F - <<'EOF'
feat(eval): candidate 매칭을 IoU 에서 onset point error 로 교체

계약 v1.1 §4-1(2026-09-10)이 span 을 coarse 후보 창으로 확정하면서 IoU
매칭이 폐기됐다. GT target 에서 외연이 사라지고 t_onset_sec 만 남으므로
_iou 는 계산 자체가 불가능해져 삭제한다.

창은 containment_rate 로만 남긴다 — 점 오차는 맞는데 창이 onset 을 안
품으면 search 쪽 창 생성이 의심되는 별개 신호다.

결과 키 span_error_sec 를 onset_error_sec 으로 개명한다. 이름이 구간
오차를 뜻하는 채로 점 오차를 담으면 반드시 오해된다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
```

---

## Task 5: plate 채점 경로를 연다

**Files:**
- Modify: `eval/runners/normalize.py` · `eval/runners/impls/mock_pack.py` · `eval/run.py` · `eval/score.py` · `eval/scorers/plate.py`
- Create: `eval/manifests/mock_pack/gt/gt_plate.json` · `tests/eval/test_scorer_plate.py`

**Interfaces:**
- Consumes: Task 2의 plate 라벨
- Produces:
  - `normalize.normalize_plate(raw)` → `[{"readout_id", "scenario_id", "source_profile", "value", "status", "abstained"}]`
  - `plate.score(normalized, gt)` → `{"exact_accuracy", "wrong_accept_rate", "abstention_recall", "n", "coverage"}`
  - `mock_pack.run({"stage": "plate"})` → 원본 `PlateReadout` dict 목록에 `scenario_id`를 얹은 것

**왜:** Task 2가 만든 참값을 소비할 경로가 지금 없다. `score.py:40`이 `plate.score(norm, None)`을 stage와 무관하게 매번 부르고 그 함수는 인자를 보지도 않고 전부 `null`을 반환한다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`tests/eval/test_scorer_plate.py` 신규:

```python
"""plate 교차표.

정답 판정은 abstained × GT legibility 다.

                 GT READABLE                    GT UNREADABLE
 abstained=false exact_match: value==true_text  wrong_accept
 abstained=true  놓친 판독                       abstention_recall 적중
"""
import pytest

from eval.scorers import plate


def _gt(*items):
    return {"meta": {"gt_version": "mp1"}, "items": list(items)}


def _truth(readout_id, legibility, true_text=None):
    out = {"readout_id": readout_id, "legibility": legibility,
           "derived_from_pack": True}
    if true_text is not None:
        out["true_text"] = true_text
    return out


def _pred(readout_id, value, abstained):
    return {"readout_id": readout_id, "scenario_id": "s", "source_profile": "p",
            "value": value, "status": "OK", "abstained": abstained}


def test_reading_a_readable_plate_correctly_is_an_exact_match():
    out = plate.score([_pred("r1", "12가3456", False)],
                      _gt(_truth("r1", "READABLE", "12가3456")))
    assert out["exact_accuracy"] == 1.0


def test_misreading_a_readable_plate_is_not_an_exact_match():
    out = plate.score([_pred("r1", "12가3457", False)],
                      _gt(_truth("r1", "READABLE", "12가3456")))
    assert out["exact_accuracy"] == 0.0


def test_abstaining_on_an_unreadable_plate_is_correct():
    """초판독의 abstain 은 실패가 아니라 정답이다."""
    out = plate.score([_pred("r1", "17나28??", True)],
                      _gt(_truth("r1", "UNREADABLE")))
    assert out["abstention_recall"] == 1.0
    assert out["wrong_accept_rate"] == 0.0


def test_confidently_reading_an_unreadable_plate_is_a_wrong_accept():
    """이 지표가 잡아야 할 오류다 — 판독 불가인데 확정값을 냈다."""
    out = plate.score([_pred("r1", "17나2867", False)],
                      _gt(_truth("r1", "UNREADABLE")))
    assert out["wrong_accept_rate"] == 1.0
    assert out["abstention_recall"] == 0.0


def test_no_gt_returns_null_not_zero():
    """데이터가 없는 것과 성능이 나쁜 것은 다른 사실이다."""
    out = plate.score([_pred("r1", "x", False)], None)
    assert out["exact_accuracy"] is None
    assert "NO_PLATE_GT" in out["coverage"]


def test_circularity_and_zero_numerator_are_written_into_coverage():
    """mock tier 에서 exact_match 는 구조상 만점이고 wrong_accept 는 분자가 0이다.

    이 두 사실이 결과 파일에 없으면 다음 사람이 숫자를 성능으로 읽는다.
    """
    out = plate.score(
        [_pred("r1", "12가3456", False), _pred("r2", "17나28??", True)],
        _gt(_truth("r1", "READABLE", "12가3456"), _truth("r2", "UNREADABLE")),
    )
    assert "순환" in out["coverage"]
    assert "분자" in out["coverage"]
```

- [ ] **Step 2: 실패를 확인한다**

Run: `python -m pytest tests/eval/test_scorer_plate.py -q`
Expected: FAIL — 현행 `plate.score`는 인자를 무시하고 전부 `None`을 반환한다

- [ ] **Step 3: 구현**

`eval/scorers/plate.py`를 전면 교체한다:

```python
"""Plate 단계 지표.

정답 판정은 abstained × GT legibility 교차표다.

                 GT READABLE                    GT UNREADABLE
 abstained=false exact_match: value==true_text  wrong_accept
 abstained=true  놓친 판독                       abstention_recall 적중

wrong_accept_rate 의 분모는 「정답이 UNREADABLE 인 항목」이다 —
abstained=true 인 예측이 아니다(harness-v1-design.md §9 F6, ADR §4.10).
판독 불가 번호판에 확신을 담아 값을 내는 것이야말로 이 지표가 잡을 오류다.

A tier 는 번호판이 비식별 처리되어 문자 정답이 없다. GT 가 없으면 0 이
아니라 null 이다 — 데이터가 없는 것과 성능이 나쁜 것은 다른 사실이다.
"""

NO_GT = ("NO_PLATE_GT — 이 manifest 에 plate 정답지가 없다. "
         "A tier 는 번호판 마스킹(harness-v1-design.md §4-3)")

CIRCULARITY = ("순환 경고 — mock tier 의 true_text 는 판독 결과에서 유도한 "
               "값이라 exact_accuracy 는 구조상 만점이다. 성능 근거가 아니다")

ZERO_NUMERATOR = ("wrong_accept_rate 분자 0건 — pack 에 「확신에 차서 틀리게 "
                  "읽은」 케이스가 없다. 안전하다는 증거가 아니다")


def not_run(reason):
    return {"exact_accuracy": None, "wrong_accept_rate": None,
            "abstention_recall": None, "n": 0, "coverage": reason}


def score(normalized, gt):
    if gt is None:
        return not_run(NO_GT)

    truth = {t["readout_id"]: t for t in gt["items"]}
    n_exact_den = n_exact_hit = 0
    n_unreadable = n_abstained = n_wrong_accept = 0
    n_scored = 0
    circular = False

    for pred in normalized:
        t = truth.get(pred["readout_id"])
        if t is None:
            continue                      # 정답지에 없는 판독은 채점 대상이 아니다
        n_scored += 1
        circular = circular or bool(t.get("derived_from_pack"))
        if t["legibility"] == "READABLE":
            if not pred["abstained"]:
                n_exact_den += 1
                if pred["value"] == t["true_text"]:
                    n_exact_hit += 1
        else:
            n_unreadable += 1
            if pred["abstained"]:
                n_abstained += 1
            else:
                n_wrong_accept += 1

    reasons = []
    if circular:
        reasons.append(CIRCULARITY)
    if n_unreadable and not n_wrong_accept:
        reasons.append(ZERO_NUMERATOR)
    if not n_scored:
        reasons.append("NO_SCORED_READOUTS — 정답지와 겹치는 판독이 없다")

    return {
        "exact_accuracy": (n_exact_hit / n_exact_den) if n_exact_den else None,
        "wrong_accept_rate": (n_wrong_accept / n_unreadable) if n_unreadable else None,
        "abstention_recall": (n_abstained / n_unreadable) if n_unreadable else None,
        "n": n_scored,
        "coverage": "; ".join(reasons) if reasons else None,
    }
```

`eval/runners/normalize.py`에 추가:

```python
def normalize_plate(raw):
    """PlateReadout 계약 목록을 판독 단위 뷰로 옮긴다.

    legibility 는 계약에 없다 — 「사람이 보면 읽히는가」는 eval 이 새로
    만드는 참값이고 정답지에만 있다. 여기서는 예측 쪽 사실만 옮긴다.
    """
    out = []
    for i, p in enumerate(raw):
        where = "normalize_plate: raw[%d]" % i
        _require_dict(p, where)
        obs = _require_field(p, "observation", where)
        _require_dict(obs, "%s.observation" % where)
        out.append({
            "readout_id": _require_field(p, "readout_id", where),
            "scenario_id": p.get("scenario_id"),
            "source_profile": (p.get("input_ref") or {}).get("source_profile"),
            "value": obs.get("value"),
            "status": obs.get("status"),
            "abstained": _require_field(p, "abstained", where),
        })
    return out
```

`eval/runners/impls/mock_pack.py`의 `run()`에 분기를 더하고 헬퍼를 추가한다:

```python
def run(scope):
    if scope["stage"] == "candidate":
        return _run_candidate()
    if scope["stage"] == "plate":
        return _run_plate()
    raise ValueError(
        "mock_pack:contracts 는 stage=candidate|plate 만 지원한다 (받은 값: %r)."
        % scope["stage"]
    )


def _run_plate():
    out = []
    for scenario in SCENARIOS:
        doc = _read("readout", scenario + ".json")
        if doc is None:
            continue
        for p in doc.get("plate_readouts", []):
            item = dict(p)
            item["scenario_id"] = scenario
            out.append(item)
    return out
```

`eval/run.py`의 `_NORMALIZERS`에 한 줄 더한다:

```python
    "plate": normalize.normalize_plate,
```

`eval/score.py`의 `build_result`를 고친다 — `plate`도 stage로 분기하고 GT를 넘긴다:

```python
def build_result(env):
    stage = env["meta"]["stage"]
    manifest = env["meta"]["manifest"]
    gt = manifests_io.load_gt(manifest, stage)
    norm = env["normalized"]

    result = {
        "meta": {...기존 그대로...},
        "candidate": None,
        "classification": None,
        "plate": None,
    }
    if stage == "candidate":
        result["candidate"] = candidate.score(norm, gt)
        result["classification"] = classification.not_run("NOT_RUN — stage=candidate 실행이다")
        result["plate"] = plate.not_run("NOT_RUN — stage=candidate 실행이다")
    elif stage == "classification":
        result["classification"] = classification.score(norm, gt)
        result["candidate"] = candidate.not_run("NOT_RUN — stage=classification 실행이다")
        result["plate"] = plate.not_run("NOT_RUN — stage=classification 실행이다")
    elif stage == "plate":
        result["plate"] = plate.score(norm, gt)
        result["candidate"] = candidate.not_run("NOT_RUN — stage=plate 실행이다")
        result["classification"] = classification.not_run("NOT_RUN — stage=plate 실행이다")
    return result
```

`eval/manifests/mock_pack/gt/gt_plate.json` 신규:

```json
{
  "meta": {
    "gt_version": "mp1",
    "tier": "MOCK",
    "stage": "plate",
    "coverage": {
      "readouts_total": 5,
      "readable": 4,
      "unreadable": 1,
      "derived_from_mock_pack": true,
      "independent_ground_truth": false,
      "purpose": "plate 지표의 배관(normalize -> score -> results)이 실제 참값을 태우고 도는지 확인한다.",
      "derived_from": ["data/mock/expected/ (eval-expected/v2 plate_readout 라벨)"],
      "warning": "true_text 가 채점 대상인 판독 결과(observation.value)에서 왔다. 따라서 exact_accuracy 는 구조상 만점이며 성능 근거가 될 수 없다. wrong_accept_rate 는 분자가 0건이다 — pack 에 「확신에 차서 틀리게 읽은」 케이스가 없어 이 수치는 안전하다는 증거가 아니다.",
      "not_scored": [
        "scenario_infra_failure_001 — READOUT_INFRA_FAILURE (판독 0건)",
        "scenario_relative_rebase_001 — NO_PLATE_READOUT"
      ],
      "reviewed_by": "daewon",
      "reviewed_at": "2026-09-13"
    }
  },
  "items": [
    { "readout_id": "readout_h001_plate", "scenario_id": "scenario_happy_001",
      "legibility": "READABLE", "true_text": "12가3456", "derived_from_pack": true },
    { "readout_id": "readout_p001_plate", "scenario_id": "scenario_plate_reread_001",
      "legibility": "UNREADABLE", "derived_from_pack": true },
    { "readout_id": "readout_p001_plate_reread", "scenario_id": "scenario_plate_reread_001",
      "legibility": "READABLE", "true_text": "17나2867", "derived_from_pack": true },
    { "readout_id": "readout_r001_plate", "scenario_id": "scenario_correction_rerun_001",
      "legibility": "READABLE", "true_text": "34나7890", "derived_from_pack": true },
    { "readout_id": "readout_u001_plate", "scenario_id": "scenario_unknown_abstain_partial_001",
      "legibility": "READABLE", "true_text": "88부1234", "derived_from_pack": true }
  ]
}
```

- [ ] **Step 4: 통과를 확인한다**

Run: `python -m pytest tests/eval -q`
Expected: 0 failed

Run (실제 배관):
```bash
python -m eval.run --impl mock_pack:contracts --manifest mock_pack --stage plate --run-id t_plate_check
python -m eval.score --prediction t_plate_check
python -c "import json,io,sys; sys.stdout=io.TextIOWrapper(sys.stdout.buffer,encoding='utf-8'); print(json.dumps(json.load(open('eval/results/t_plate_check.mp1.json',encoding='utf-8'))['plate'],ensure_ascii=False,indent=2))"
```
Expected: `exact_accuracy: 1.0` · `abstention_recall: 1.0` · `wrong_accept_rate: 0.0` · `n: 5` · `coverage`에 순환 경고와 분자 0건 경고가 둘 다 있다

확인 후 검사용 산출물을 지운다: `rm eval/predictions/t_plate_check.json eval/results/t_plate_check.mp1.json`

- [ ] **Step 5: 커밋**

```bash
git add eval/scorers/plate.py eval/runners/normalize.py eval/runners/impls/mock_pack.py \
        eval/run.py eval/score.py eval/manifests/mock_pack/gt/gt_plate.json tests/eval/
git commit -F - <<'EOF'
feat(eval): plate 채점 경로를 연다 (F6 해소)

--stage plate 와 normalize_plate 를 추가하고 plate.score 의 GT 분기를
재구현한다. 검증할 GT 가 5건 있으므로 F6 교훈(검증할 GT 가 없는 분기를
만들지 않는다)의 조건은 충족된다.

판정은 abstained x GT legibility 교차표다. wrong_accept_rate 의 분모는
「정답이 UNREADABLE 인 항목」이지 abstained=true 인 예측이 아니다.

mock tier 의 한계를 결과 coverage 에 적는다 — true_text 가 판독 결과에서
왔으므로 exact_accuracy 는 구조상 만점이고, wrong_accept_rate 는 분자가
0건이라 안전하다는 증거가 아니다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
```

---

## Task 6: 결과 envelope 누락 4건

**Files:**
- Modify: `eval/run.py` · `eval/score.py` · `eval/manifests_io.py` · `eval/runners/impls/mock_pack.py`
- Test: `tests/eval/test_run.py` · `tests/eval/test_score_cli.py`

**Interfaces:**
- Consumes: Task 4의 `candidate.SCORER_VERSION`
- Produces:
  - impl 선택 훅 `run_facts(scope) -> dict` — 있으면 `build_envelope`가 부른다. 키: `contract_version`(str|None) · `processed_duration_sec`(float|None) · `usage_records`(list) · `scenarios`(list)
  - envelope `meta`에 `contract_version` · `processed_duration_sec`
  - envelope 최상위 `facts` 블록
  - result `meta`에 `scorer_version` · `prediction_ref: {"path", "sha256"}`
  - `manifests_io.sha256_file(path) -> str`

**왜:** 지금은 계약이 바뀐 전후의 결과가 구분되지 않고, IoU→point error 전환 전후도 파일상 구분되지 않으며, 예측을 덮어써도 결과가 그 사실을 모른다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`tests/eval/test_run.py`에 추가:

```python
def test_envelope_records_the_contract_version_and_processed_duration(tmp_path, monkeypatch):
    """어느 계약을 태운 실행인지, 원본 몇 초를 처리했는지가 남아야 한다.

    없으면 계약이 바뀐 전후의 결과를 구분할 수 없고, 시간당 환산치를
    결과 파일만으로 재현할 수 없다.
    """
    monkeypatch.setattr(paths, "predictions_dir", lambda: str(tmp_path / "predictions"))
    assert run.main(["--impl", "mock_pack:contracts", "--manifest", "mock_pack",
                     "--stage", "candidate", "--run-id", "t_env"]) == 0

    env = json.loads((tmp_path / "predictions" / "t_env.json").read_text(encoding="utf-8"))
    assert env["meta"]["contract_version"] == "analysis-run-candidate-event/v1.1"
    # 전후방 2소스를 합산하지 않는다 (1차 A절 P2-9)
    assert env["meta"]["processed_duration_sec"] == 9300.0
    assert env["facts"]["usage_records"]


def test_fake_impls_declare_no_contract_version(tmp_path, monkeypatch):
    """훅이 없는 impl 은 null 이다. 지어내지 않는다."""
    monkeypatch.setattr(paths, "predictions_dir", lambda: str(tmp_path / "predictions"))
    assert run.main(["--impl", "fake:always_correct", "--manifest", "b_youtube",
                     "--stage", "candidate", "--run-id", "t_fake_env"]) == 0

    env = json.loads((tmp_path / "predictions" / "t_fake_env.json").read_text(encoding="utf-8"))
    assert env["meta"]["contract_version"] is None
    assert env["meta"]["processed_duration_sec"] is None
```

`tests/eval/test_score_cli.py`에 추가:

```python
def test_result_pins_the_scorer_version_and_the_prediction_it_scored(tmp_path, monkeypatch):
    """어느 지표 정의로 어느 예측을 채점했는지가 결과에 남아야 한다.

    run_id 문자열이 같다는 것만으로 이어져 있으면, 예측 파일을 덮어써도
    결과가 그 사실을 모른다.
    """
    monkeypatch.setattr(paths, "predictions_dir", lambda: str(tmp_path / "predictions"))
    monkeypatch.setattr(paths, "results_dir", lambda: str(tmp_path / "results"))
    assert run.main(["--impl", "mock_pack:contracts", "--manifest", "mock_pack",
                     "--stage", "candidate", "--run-id", "t_pin"]) == 0
    assert score.main(["--prediction", "t_pin"]) == 0

    result = json.loads((tmp_path / "results" / "t_pin.mp1.json").read_text(encoding="utf-8"))
    assert result["meta"]["scorer_version"] == "s2"
    ref = result["meta"]["prediction_ref"]
    assert ref["path"].endswith("t_pin.json")
    assert len(ref["sha256"]) == 64


def test_score_refuses_when_the_contract_version_does_not_match(tmp_path, monkeypatch):
    """버전이 다르면 비교를 거부한다 (module-architecture v4 §9-2 규칙 5)."""
    monkeypatch.setattr(paths, "predictions_dir", lambda: str(tmp_path / "predictions"))
    monkeypatch.setattr(paths, "results_dir", lambda: str(tmp_path / "results"))
    assert run.main(["--impl", "mock_pack:contracts", "--manifest", "mock_pack",
                     "--stage", "candidate", "--run-id", "t_mismatch"]) == 0

    path = tmp_path / "predictions" / "t_mismatch.json"
    env = json.loads(path.read_text(encoding="utf-8"))
    env["meta"]["contract_version"] = "analysis-run-candidate-event/v9.9"
    path.write_text(json.dumps(env, ensure_ascii=False), encoding="utf-8")

    assert score.main(["--prediction", "t_mismatch"]) == 4
```

- [ ] **Step 2: 실패를 확인한다**

Run: `python -m pytest tests/eval/test_run.py tests/eval/test_score_cli.py -q`
Expected: FAIL — `KeyError: 'contract_version'` 등

- [ ] **Step 3: 구현**

`eval/manifests_io.py`에서 `_sha256`을 공개한다:

```python
def sha256_file(path):
    h = hashlib.sha256()
    with open(path, "rb") as f:
        for chunk in iter(lambda: f.read(1024 * 1024), b""):
            h.update(chunk)
    return h.hexdigest()
```

기존 `_sha256` 정의를 지우고 110~114줄의 호출부를 `sha256_file(p)`로 바꾼다.

`eval/runners/impls/mock_pack.py`에 훅을 추가한다:

```python
CONTRACT_VERSIONS = {
    "candidate": "analysis-run-candidate-event/v1.1",
    "plate": "plate-readout/v1.2",
}


def _timeline_union_sec(scenario):
    """timeline 이 덮는 원본 길이. 전후방 2소스를 합산하지 않는다.

    happy_001 은 전후방이 둘 다 0~1200s 라 합산하면 2400s 로 2배가 된다
    (1차 A절 P2-9). revision 이 여럿이면 최신 것만 본다.
    """
    doc = _read("recording", scenario + ".json")
    if doc is None:
        return 0.0
    timelines = doc.get("recording_timelines", [])
    if not timelines:
        return 0.0
    latest = max(timelines, key=lambda t: t.get("revision", 0))
    spans = [(p["timeline_start_sec"], p["timeline_end_sec"])
             for p in latest.get("source_placements", [])]
    total = 0.0
    cur_start = cur_end = None
    for s, e in sorted(spans):
        if cur_end is None or s > cur_end:
            if cur_end is not None:
                total += cur_end - cur_start
            cur_start, cur_end = s, e
        else:
            cur_end = max(cur_end, e)
    if cur_end is not None:
        total += cur_end - cur_start
    return total


def _scenarios_for(stage):
    """이 stage 가 실제로 예측을 만든 시나리오들."""
    if stage == "candidate":
        return [s for s in SCENARIOS if _candidate_events(s) is not None]
    if stage == "plate":
        return [s for s in SCENARIOS if _read("readout", s + ".json") is not None]
    return []


def run_facts(scope):
    """이 실행이 처리한 원본과 비용 재료. run.py 가 선택적으로 부른다.

    분자(비용)와 분모(원본 길이)를 같은 시나리오 집합으로 맞춘다 —
    대상 아님으로 뺀 시나리오는 양쪽 모두에서 빠진다.
    """
    stage = scope["stage"]
    scenarios = _scenarios_for(stage)
    usage = []
    for scenario in scenarios:
        doc = _read("common", scenario + ".json")
        if doc is None:
            continue
        usage.extend(doc.get("usage_records", []))
    return {
        "contract_version": CONTRACT_VERSIONS.get(stage),
        "processed_duration_sec": sum(_timeline_union_sec(s) for s in scenarios),
        "usage_records": usage,
        "scenarios": scenarios,
    }
```

`eval/run.py`의 `build_envelope`에 훅 호출을 더한다:

```python
    # 훅이 없는 impl 은 null 이다 — 값을 지어내지 않는다.
    facts_fn = getattr(impl_module, "run_facts", None)
    facts = facts_fn(scope) if facts_fn is not None else {}
```

`meta`에 두 줄, 반환 dict에 한 줄을 더한다:

```python
            "contract_version": facts.get("contract_version"),
            "processed_duration_sec": facts.get("processed_duration_sec"),
```

```python
        "facts": facts,
```

`eval/score.py`에 검사를 더한다. **import 줄은 이 Task에서 건드리지 않는다** — `cost` 모듈은 Task 7에서 만들므로 지금 추가하면 `ModuleNotFoundError`가 난다.

`build_result`의 `meta`에 두 줄을 더한다:

```python
            "scorer_version": candidate.SCORER_VERSION,
            "prediction_ref": _prediction_ref(env["meta"]["run_id"]),
```

`score.py`에 헬퍼와 거부 검사를 더한다:

```python
def _prediction_ref(run_id):
    path = os.path.join(paths.predictions_dir(), run_id + ".json")
    return {"path": os.path.relpath(path, paths.REPO_ROOT).replace("\\", "/"),
            "sha256": manifests_io.sha256_file(path)}


class ContractMismatch(Exception):
    pass
```

`build_result` 첫머리에:

```python
    gt_contract = gt["meta"].get("contract_version")
    env_contract = env["meta"].get("contract_version")
    if gt_contract and env_contract and gt_contract != env_contract:
        raise ContractMismatch(
            "계약 버전이 다르면 비교하지 않는다 (v4 §9-2 규칙 5): "
            "정답지=%s · 예측=%s" % (gt_contract, env_contract))
```

`main()`의 예외 처리에 분기를 더한다:

```python
    except ContractMismatch as e:
        print("실패: %s" % e, file=sys.stderr)
        return 4
```

`gt_candidate.json`·`gt_plate.json`의 `meta`에 각각 한 줄을 더한다:

```json
    "contract_version": "analysis-run-candidate-event/v1.1",
```
```json
    "contract_version": "plate-readout/v1.2",
```

- [ ] **Step 4: 통과를 확인한다**

Run: `python -m pytest tests/eval -q`
Expected: 0 failed

`processed_duration_sec`가 9300.0이 아니면 union 계산을 확인한다 — candidate stage는 `infra_failure_001`(1200s)이 빠져 `1200 + 1800 + 1800 + 1500 + 1800 + 1200 = 9300`이다.

- [ ] **Step 5: 커밋**

```bash
git add eval/run.py eval/score.py eval/manifests_io.py eval/runners/impls/mock_pack.py \
        eval/manifests/mock_pack/gt/ tests/eval/
git commit -F - <<'EOF'
feat(eval): envelope 에 contract_version·processed_duration_sec·scorer_version·prediction_ref

결과 파일만으로 「어느 계약 · 어느 지표 정의 · 어느 예측」을 채점했는지
재현할 수 있게 한다. run_id 문자열이 같다는 것만으로 이어져 있으면
예측을 덮어써도 결과가 그 사실을 모른다 — sha256 으로 묶는다.

계약 버전이 다르면 score 가 비교를 거부한다(exit 4, v4 §9-2 규칙 5).
지금까지는 근거값 자체가 결과에 없어 규칙을 강제할 수단이 없었다.

processed_duration_sec 는 timeline union 이다. 전후방 2소스를 합산하지
않는다 — happy_001 은 둘 다 0~1200s 라 합산하면 2배가 된다(P2-9).

훅이 없는 impl 은 null 이다. 값을 지어내지 않는다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
```

---

## Task 7: 비용 지표 블록

**Files:**
- Create: `eval/scorers/cost.py` · `tests/eval/test_scorer_cost.py`
- Modify: `eval/score.py`

**Interfaces:**
- Consumes: Task 6의 `env["facts"]["usage_records"]` · `env["meta"]["processed_duration_sec"]`
- Produces: `cost.score(usage_records, processed_duration_sec, scenarios) -> dict` · result 최상위 `cost` 블록

**왜:** 재료가 이미 다 있다 — `UsageRecord` 26행, 정본 집계 키 `case_id` 확정, KRW 정규화 완료. **집계 키를 `run_ref`로 잡으면 STALE attempt row 4건이 빠진다.**

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`tests/eval/test_scorer_cost.py` 신규:

```python
"""비용 집계.

정본 집계 키는 case_id 다 (usage-record/v1.2 §9-2). run_ref 로 묶으면
run 이 산출되지 않은 attempt(run_ref=null, RUN_NOT_PRODUCED)가 통째로
빠져 비용이 과소 보고된다.
"""
import pytest

from eval.scorers import cost


def _row(case_id, amount, currency="KRW", run_ref=None, reason=None):
    return {"case_id": case_id, "cost": {"amount": amount, "currency": currency},
            "run_ref": run_ref, "run_ref_reason": reason}


def test_aggregates_by_case_id_not_by_run_ref():
    rows = [
        _row("case_a", "100", run_ref={"kind": "analysis_run", "ref": "run_a"}),
        _row("case_a", "50", run_ref=None, reason="RUN_NOT_PRODUCED"),
        _row("case_b", "30", run_ref=None, reason="DIRECT_NO_RUN"),
    ]
    out = cost.score(rows, processed_duration_sec=3600.0, scenarios=["s1"])

    assert out["cost_per_case"] == {"case_a": 150.0, "case_b": 30.0}
    assert out["total"] == 180.0
    assert out["currency"] == "KRW"


def test_cost_per_source_video_hour_uses_the_given_denominator():
    rows = [_row("case_a", "1200")]
    out = cost.score(rows, processed_duration_sec=3600.0, scenarios=["s1"])
    assert out["cost_per_source_video_hour"] == pytest.approx(1200.0)


def test_zero_cost_case_is_kept_not_dropped():
    """0원 case 를 빼면 분포가 왜곡된다 — 평균만 보면 안 보인다."""
    rows = [_row("case_a", "100"), _row("case_x", "0")]
    out = cost.score(rows, processed_duration_sec=3600.0, scenarios=["s1"])
    assert out["cost_per_case"]["case_x"] == 0.0


def test_mixed_currency_is_refused_not_converted():
    """환율은 pricing_context 에 귀속되지 eval 이 정할 값이 아니다."""
    rows = [_row("case_a", "100", "KRW"), _row("case_b", "1", "USD")]
    out = cost.score(rows, processed_duration_sec=3600.0, scenarios=["s1"])

    assert out["total"] is None
    assert "MIXED_CURRENCY" in out["coverage"]


def test_no_usage_rows_returns_null_not_zero():
    out = cost.score([], processed_duration_sec=3600.0, scenarios=[])
    assert out["total"] is None
    assert "NO_USAGE_RECORDS" in out["coverage"]


def test_no_duration_leaves_the_hourly_rate_null():
    rows = [_row("case_a", "100")]
    out = cost.score(rows, processed_duration_sec=None, scenarios=["s1"])
    assert out["total"] == 100.0
    assert out["cost_per_source_video_hour"] is None
```

- [ ] **Step 2: 실패를 확인한다**

Run: `python -m pytest tests/eval/test_scorer_cost.py -q`
Expected: FAIL — `ModuleNotFoundError: No module named 'eval.scorers.cost'`

- [ ] **Step 3: 구현**

`eval/scorers/cost.py` 신규:

```python
"""비용 지표.

정본 집계 키는 case_id 다 (contract-usage-record.md §9-2). run_ref 는
per-run 감사용이며, run 이 산출되지 않은 attempt(run_ref=null,
RUN_NOT_PRODUCED)는 run_ref 로 묶으면 통째로 빠져 비용이 과소 보고된다.

분모(processed_duration_sec)는 호출부가 넘긴다. 이 모듈은 파일을 읽지
않는다 — 분자와 분모가 같은 시나리오 집합에서 나왔는지는 넘기는 쪽이
보장한다.
"""

NO_ROWS = "NO_USAGE_RECORDS — 이 실행에 비용 기록이 없다"
MIXED = ("MIXED_CURRENCY — 통화가 섞여 합산하지 않는다. 환율은 "
         "pricing_context 에 귀속되며 eval 이 정할 값이 아니다")


def score(usage_records, processed_duration_sec, scenarios):
    if not usage_records:
        return {"cost_per_case": None, "cost_per_source_video_hour": None,
                "total": None, "currency": None, "n_rows": 0,
                "scenarios": list(scenarios), "coverage": NO_ROWS}

    currencies = {r["cost"]["currency"] for r in usage_records}
    if len(currencies) > 1:
        return {"cost_per_case": None, "cost_per_source_video_hour": None,
                "total": None, "currency": sorted(currencies),
                "n_rows": len(usage_records), "scenarios": list(scenarios),
                "coverage": MIXED}

    per_case = {}
    for r in usage_records:
        case_id = r["case_id"]
        per_case[case_id] = per_case.get(case_id, 0.0) + float(r["cost"]["amount"])

    total = sum(per_case.values())
    hourly = None
    if processed_duration_sec:
        hourly = total / (processed_duration_sec / 3600.0)

    reasons = []
    zero_cases = sorted(c for c, v in per_case.items() if v == 0.0)
    if zero_cases:
        # 평균만 보면 안 보인다. 0원 case 가 있다는 사실을 파일에 남긴다.
        reasons.append("ZERO_COST_CASES — %s" % ", ".join(zero_cases))
    if processed_duration_sec is None:
        reasons.append("NO_PROCESSED_DURATION — 시간당 환산치를 낼 수 없다")

    return {
        "cost_per_case": per_case,
        "cost_per_source_video_hour": hourly,
        "total": total,
        "currency": currencies.pop(),
        "n_rows": len(usage_records),
        "scenarios": list(scenarios),
        "coverage": "; ".join(reasons) if reasons else None,
    }
```

`eval/score.py`의 import에 `cost`를 더하고, `build_result`의 반환 dict에 블록을 더한다:

```python
        "cost": cost.score(
            (env.get("facts") or {}).get("usage_records", []),
            env["meta"].get("processed_duration_sec"),
            (env.get("facts") or {}).get("scenarios", []),
        ),
```

- [ ] **Step 4: 통과를 확인한다**

Run: `python -m pytest tests/eval -q`
Expected: 0 failed

Run (실측 확인):
```bash
python -m eval.run --impl mock_pack:contracts --manifest mock_pack --stage candidate --run-id t_cost_check
python -m eval.score --prediction t_cost_check
python -c "import json,io,sys; sys.stdout=io.TextIOWrapper(sys.stdout.buffer,encoding='utf-8'); print(json.dumps(json.load(open('eval/results/t_cost_check.mp1.json',encoding='utf-8'))['cost'],ensure_ascii=False,indent=2))"
```
Expected: `currency: "KRW"` · `cost_per_case`에 `case_h001: 938.0` 등이 보이고 · `cost_per_source_video_hour`가 실수 · `coverage`에 `ZERO_COST_CASES`가 있으면 그 case가 실제로 0원인지 확인한다(`case_x001`은 candidate stage에서 빠지므로 나타나지 않는 것이 정상이다)

지운다: `rm eval/predictions/t_cost_check.json eval/results/t_cost_check.mp1.json`

- [ ] **Step 5: 커밋**

```bash
git add eval/scorers/cost.py eval/score.py tests/eval/test_scorer_cost.py
git commit -F - <<'EOF'
feat(eval): 결과에 cost 블록 추가 — case_id 로 집계한다

정본 집계 키는 case_id 다(usage-record/v1.2 §9-2). run_ref 로 묶으면
run 이 산출되지 않은 attempt(run_ref=null, RUN_NOT_PRODUCED)가 통째로
빠져 비용이 과소 보고된다.

통화가 섞이면 환산하지 않고 거부한다 — 환율은 pricing_context 에
귀속되며 eval 이 정할 값이 아니다.

0원 case 를 빼지 않고 case 별로 남겨 분포가 보이게 한다. 평균만 보면
0원 case 가 있다는 사실이 사라진다.

scorer 는 파일을 읽지 않는다. 분자와 분모를 같은 시나리오 집합으로
맞추는 책임은 impl 의 run_facts 에 있다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
```

---

## Task 8: `clip_id` 규약 명문화와 문서 문구 2곳

**Files:**
- Modify: `docs/modules/eval/experiment-guide.md:483` 부근 · `docs/modules/eval/harness-v1-design.md:267`
- Test: `tests/eval/test_mock_pack_pipeline.py`

**Interfaces:**
- Consumes: Task 3의 `gt_candidate.json` `meta.clip_id_convention`
- Produces: 없음(문서와 단언만)

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`tests/eval/test_mock_pack_pipeline.py`에 추가:

```python
def test_gt_states_the_clip_id_convention_in_the_file():
    """규약이 코드 주석이 아니라 결과에서 읽히는 자리에 있어야 한다.

    B tier 는 clip_id 가 실재하고 mock tier 는 시나리오다. 같은 scorer 를
    타므로 결과 파일만 보고 어느 쪽인지 말할 수 있어야 한다.
    """
    for stage in ("candidate", "plate"):
        meta = manifests_io.load_gt(MANIFEST, stage)["meta"]
        assert meta.get("contract_version")
    cand_meta = manifests_io.load_gt(MANIFEST, "candidate")["meta"]
    assert "scenario_id" in cand_meta["clip_id_convention"]
    assert "clip_id" in cand_meta["clip_id_convention"]
```

- [ ] **Step 2: 실패를 확인한다**

Run: `python -m pytest tests/eval/test_mock_pack_pipeline.py -q -k clip_id_convention`
Expected: Task 3에서 이미 넣었다면 PASS — 그러면 이 Step은 「회귀 방지 단언을 남겼다」로 통과 처리하고 넘어간다. FAIL이면 `gt_candidate.json`의 `meta.clip_id_convention`을 확인한다.

- [ ] **Step 3: 문서 2곳을 고친다**

`docs/modules/eval/experiment-guide.md` — 483줄의 `| OVERCONFIDENT | 불확실하지만 확정값 반환 |`이 속한 표 **바로 아래**에 한 줄을 넣는다:

```markdown
> 이 표는 이름 6개를 한 자리에 모은 것이고 층위가 둘이다 — **런타임 실패 5종**(실행 중 관측되는 사실)과 **사후 분류 1종(`OVERCONFIDENT`)**. 후자는 GT가 있어야 판정되므로 런타임에는 존재하지 않는다. 9/7 ADR §4.10의 「`failure.kind`를 6개로 닫는다」는 이 층위 분리로 읽는다.
```

`docs/modules/eval/harness-v1-design.md` 267줄 끝의 문장을 고친다. **의미를 바꾸지 않는다 — 용어만 맞춘다.** 원문의 「abstain 항목(판독 불가로 답이 없는 번호판)을 **포함**해야 한다」를 이렇게 바꾼다:

```markdown
C tier로 재구현할 때는 `wrong_accept_rate`의 분모를 **「정답(GT)이 `UNREADABLE`인 항목」**으로 잡아야 한다 — 판독 불가 번호판에 대한 잘못된 인식이야말로 이 지표가 잡아야 할 오류이기 때문이다. 분모는 예측이 `abstained=true`인 항목이 **아니다**. (교차표는 `eval/scorers/plate.py` 모듈 docstring, ADR §4.10과 같은 정의다.)
```

- [ ] **Step 4: 확인한다**

Run: `python -m pytest tests/eval -q`
Expected: 0 failed

Run: `grep -n "abstained=true" docs/modules/eval/harness-v1-design.md`
Expected: 새 문장의 「분모는 예측이 `abstained=true`인 항목이 아니다」가 보인다

- [ ] **Step 5: 커밋**

```bash
git add docs/modules/eval/experiment-guide.md docs/modules/eval/harness-v1-design.md tests/eval/
git commit -F - <<'EOF'
docs(eval): OVERCONFIDENT 층위 명시 + wrong_accept_rate 분모 용어 통일

experiment-guide 의 실패 이름 표에 층위 분리를 한 줄 적는다 — 런타임
5종과 사후 분류 1종(OVERCONFIDENT)은 같은 표에 있지만 다른 층위다.

harness-v1-design F6 의 의미는 이미 맞았다(괄호로 풀어 뒀다). 고친 것은
용어뿐이다 — 「abstain 항목」은 예측 쪽 어휘인데 가리키는 대상은 정답 쪽
집합이라 괄호를 못 보면 반대로 읽힌다.

clip_id 규약이 결과에서 읽히는 자리(GT meta)에 있는지 회귀 단언을 남긴다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
```

---

## 최종 검증 (전체 Task 완료 후)

```bash
python data/mock/validate_mock_pack.py
python -m pytest tests/eval -q
python scripts/check_boundaries.py
python -m eval.run   --impl mock_pack:contracts --manifest mock_pack --stage candidate --run-id mock_e2e_v2
python -m eval.run   --impl mock_pack:contracts --manifest mock_pack --stage plate     --run-id mock_plate_v2
python -m eval.score --prediction mock_e2e_v2
python -m eval.score --prediction mock_plate_v2
```

**2차 완료 판정** — 아래가 전부 참이어야 한다.

- [ ] `validate_mock_pack.py` PASSED (exit 0), `[EVAL]` 오류 0건, **스크립트를 수정하지 않았다**
- [ ] `pytest tests/eval` **0 failed**
- [ ] `results/*.json`의 `candidate`에 `onset_error_sec` · `containment_rate` · `excluded_by_reason`가 있고 `span_error_sec`는 없다
- [ ] `results/*.json`의 `plate`가 실제 수치를 내고 `coverage`에 순환 경고와 분자 0건 경고가 둘 다 있다
- [ ] `results/*.json`의 `cost`가 `case_id`별 값을 내고 `currency: "KRW"`다
- [ ] `meta`에 `contract_version` · `processed_duration_sec` · `scorer_version` · `prediction_ref.sha256`가 있다
- [ ] `grep -rn "_iou\|span_error_sec\|candidate_events\.\|from_mock_pack" eval/ tests/` 출력이 없다

**완료 정의에서 제외** — 외부 입력 대기(`YT_0001` 원본 URL·라이선스, 자료조사 6개 문서의 `[링크 필요]`)는 이 계획의 범위가 아니다.
